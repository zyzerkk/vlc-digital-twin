# ============================================================
# VLC Digital Twin — Dependências Python
# Gerado com: pip freeze (após instalação em ambiente limpo)
# Testado com: Python 3.10, CUDA 11.8
# ============================================================

# PyTorch — instalar separadamente com suporte CUDA:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# (CPU only: --index-url https://download.pytorch.org/whl/cpu)
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0

# Ciência de dados
numpy>=1.24.0,<2.0.0
scipy>=1.11.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualização
matplotlib>=3.7.0
seaborn>=0.12.0

# Jupyter
notebook>=7.0.0
ipykernel>=6.0.0

# Utilitários
tqdm>=4.65.0
pyyaml>=6.0.0

# NVIDIA PhysicsNeMo (instalar manualmente):
# pip install nvidia-physicsnemo
# ou: git clone https://github.com/NVIDIA/physicsnemo.git && pip install -e physicsnemo

# Desenvolvimento
black>=23.0.0
flake8>=6.0.0
pytest>=7.0.0
